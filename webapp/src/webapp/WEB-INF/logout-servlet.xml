<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE beans PUBLIC "-//SPRING//DTD BEAN//EN" "http://www.springframework.org/dtd/spring-beans.dtd">

<beans>
    <import resource="config.xml"/>

    <bean id="urlMapping" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <prop key="/">logoutController</prop>
            </props>
        </property>
    </bean>

    <bean id="logoutController" class="no.kantega.security.api.impl.fedag.controller.LogoutController">
        <property name="dbAccess" ref="dbAccess"/>
        <property name="jdbcTemplate" ref="jdbcTemplate"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource">
            <bean class="org.springframework.jdbc.datasource.DriverManagerDataSource">
                <property name="driverClassName" value="${database.driver}"/>
                <property name="url" value="${database.url}"/>
                <property name="username" value="${database.username}"/>
                <property name="password" value="${database.password}"/>
            </bean>
        </property>
    </bean>

    <bean id="dbAccess" class="no.kantega.security.api.impl.fedag.database.DatabaseAccess">
        <property name="config" ref="fedagConfig"/>
    </bean>

    <bean id="fedagConfig" class="no.kantega.security.api.impl.fedag.config.Config">
        <constructor-arg index="0" ref="aksessProperties"/>
    </bean>

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="properties" ref="aksessProperties"/>
    </bean>
</beans>